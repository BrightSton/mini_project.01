import React from "react";

const Login = () => {
  return (
    <div>
      <h1>로그인</h1>
      <input type="text" />
      <input type="text" />
      <button>로그인</button>
    </div>
  );
};

export default Login;
